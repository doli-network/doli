# Redesign Intake — INC-I-104

_Auto-generated by redesign-intake-gate.sh at 2026-06-01T16:07:42Z_

## Incident Record (from .omega/memory.db)
- **ID:** INC-I-104
- **Title:** ai5 n9-n12 RAM growth ~8 MB/min + sync coordinator sees 1 peer while transport reports 45
- **Status:** diagnosed
- **Severity:** high
- **Domain:** mainnet/network/sync
- **Root cause (recorded):** RocksDB defaults uncapped on 3 of 4 instances. Direct RocksDB LOG dump on ai5/n9 shows: state_db, block_store, utxo_store all have db_write_buffer_size=0 (no total memtable budget), per-CF write_buffer_size=67108864 (64 MB), max_write_buffer_number=2. Only the diagnostics DB has db_write_buffer_size=8388608 (8 MB cap from the May 30 fix commit f37febcf). block_store and utxo_store also have max_total_wal_size=0 (uncapped WAL). The 65,536 kB anonymous mappings observed in /proc/PID/smaps across all running mainnet nodes are these uncapped memtables. Per-node natural ceiling = sum across active CFs of write_buffer_size * max_write_buffer_number. ai5 plateaus at ~450 MB/node (4 nodes x 450 = 1.8 GB on 3.7 GB box, fits). Family server fails because 6 nodes * 450 MB = 2.7 GB > 1.9 GB total RAM; OOM killer fires during memtable warm-up before steady state is reached. Pattern A (May 27-30 ai5/folsi growth to 1+ GB) was the same shape on the diagnostic_ledger DB; the f37febcf commit capped that one but the identical shape on the other 3 DBs was never fixed.

## Description
On 2026-05-31 ~22:14 UTC, all 4 producers on ai5 (n9-n12) showed RSS growing ~8 MB/min/node linearly with host available RAM dropping 2.4 GB -> 1.3 GB -> 675 MB over 36 minutes; no swap configured. Layered split observed on n12: TRANSPORT healthy (33-44 ESTABLISHED sockets, getNetworkInfo peerCount=45) vs SYNC/GOSSIP layer reports peers=1 with InsufficientPeers blocking production every slot. Coor

## Diagnosis Report Status
**State:** No diagnosis-report.md on disk. Incident has no recorded synthesizer verdict.

## Redesign-Recurrence
**Prior redesigns in this domain (last 365 days):** none.

**Prior resolved incidents in this domain (last 180 days):** none.
